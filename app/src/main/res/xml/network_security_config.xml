<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config:
    - Debug builds: trust user-installed CAs (needed for MITM in dev)
    - Release builds: system CAs only (our CA gets installed via user prompt)
    Note: For full HTTPS interception on Android 7+, the app's CA must be
    installed as a system CA (requires root) or the target app must trust
    user CAs via its own network_security_config.
-->
<network-security-config>

    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-installed CAs in debug mode (our MITM CA) -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <!-- In release, user must manually install our CA as trusted -->
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only to localhost proxy -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.0.2</domain>
    </domain-config>

</network-security-config>
